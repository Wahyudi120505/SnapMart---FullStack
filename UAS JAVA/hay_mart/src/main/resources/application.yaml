spring:
  application:
    name: hay_mart

  datasource:
    url: jdbc:mysql://localhost:3306/db_hay_mart?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 2MB


mail:
  host: smtp.gmail.com
  port: 587
  username: ayundaputri1243@gmail.com
  password: nqqz lckf sywy xlmx
  protocol: smtp
  auth: true
  starttls: true
  debug: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

app:
  verification:
    url: http://localhost:8080/auth/verify
